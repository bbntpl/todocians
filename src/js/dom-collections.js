import { createEl, appendChildren } from '../helpers';

//import icons
import { chevronRight } from '../assets/icons/chevron-left.svg';
import { projectIcon } from '../assets/icons/project.svg';
import { tagIcon } from '../assets/icons/tag.svg';

const Sidebar = (() => {
    const ribbonEl = createEl('div', 'ribbon');
    const collapseBtn = createEl('div', 'collapse-wrapper');
    const ribbonFolders = createEl('div', 'ribbon-folders');

    //image elements
    const collapseIcon = createEl('img', 'ribbon-icons');
    const prjIcon = createEl('img', 'ribbon-icons');
    const tagIcon = createEl('img', 'ribbon-icons');

})();
const Ribbon = (() => {
    const 
})();

const Header = (() => {

})();
const Main = (() => {

})();
const Modal = (() => {

})();
const Footer = (() => {

})();
export { Sidebar, Ribbon, Header, Main, Footer, Modal }